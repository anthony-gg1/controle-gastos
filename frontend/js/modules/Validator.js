class Validator {

    // Método para validar um nmero par (Exemplo)
    static validateOdd(value) {
        if (value % 2 == 0) {
            return true;
        } else {
            return false;
        }
    }

}