class User {

    // Métodos para Requisição
    static getData() {
        // Puxar dados
        return {};
    }
    static setData(data) {
        // Puxar definir dados
    }

}

class Goal {

    // Métodos para buscar registros
    static findAll() {
        // Listar registros
        return [];
    }
    static findById(id) {
        // Buscar registros
        return {};
    }

    // Métodos para realizar alterações nos registros
    static save(data) {
        // Salvar registro
    }
    static update(data) {
        // Atualizar registro
    }
    static delete(id) {
        // Apagar registro
    }

}

class Transaction {
   
    // Métodos para buscar registros
    static findAll() {
        // Listar registros
        return [];
    }
    static findById(id) {
        // Buscar registros
        return {};
    }

    // Métodos para realizar alterações nos registros
    static save(data) {
        // Salvar registro
    }
    static update(data) {
        // Atualizar registro
    }
    static delete(id) {
        // Apagar registro
    }

}